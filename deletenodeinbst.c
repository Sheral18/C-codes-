#include<stdio.h>
struct Node *delete(struct Node* root,int value)
{
    if(value<root->data)
}
int main()
{
struct node* p =NULL;
p = create(5);
p->left = create(3);
p->right = create(6);
p->left->left = create(1);
p->left->right = create(4);

inorder(p);
return 0;
}